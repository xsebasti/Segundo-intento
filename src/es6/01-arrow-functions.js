const multiplo = (numero) => {
    return numero * numero
 } // de manera explicita retornando la expreesion y haciendo el codigo mas largo

 const suma = numero => numero + numero
 // de manera implicita lo que hace la linea mas corta y mas legible